export const degreesToRadians = degrees => degrees * (Math.PI / 180)

export const randomDouble = () => Math.random()

export const randomDoubleInRange = (min, max) => min + (Math.random() * (max - min))